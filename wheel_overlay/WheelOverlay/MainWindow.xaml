<Window x:Class="WheelOverlay.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WheelOverlay"
        xmlns:vm="clr-namespace:WheelOverlay.ViewModels"
        xmlns:cv="clr-namespace:WheelOverlay.Converters"
        xmlns:v="clr-namespace:WheelOverlay.Views"
        mc:Ignorable="d"
<Window x:Class="WheelOverlay.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        <ResourceDictionary>
            <vm:OverlayViewModel x:Key="DesignViewModel" />
            <cv:IsSelectedConverter x:Key="IsSelectedConverter" />
            
            <DataTemplate x:Key="SingleTextTemplate">
                <v:SingleTextLayout />
            </DataTemplate>
            <DataTemplate x:Key="VerticalTemplate">
                <v:VerticalLayout />
            </DataTemplate>
            <DataTemplate x:Key="HorizontalTemplate">
                <v:HorizontalLayout />
            </DataTemplate>
            <DataTemplate x:Key="GridTemplate">
                <v:GridLayout />
            </DataTemplate>
        </ResourceDictionary>
    </Window.Resources>

    <Window.Resources>
        <ResourceDictionary>
            <vm:OverlayViewModel x:Key="DesignViewModel" />
            <cv:IsSelectedConverter x:Key="IsSelectedConverter" />
            
            <DataTemplate x:Key="SingleTextTemplate">
                <v:SingleTextLayout />
            </DataTemplate>
            <DataTemplate x:Key="VerticalTemplate">
                <v:VerticalLayout />
            </DataTemplate>
            <DataTemplate x:Key="HorizontalTemplate">
                <v:HorizontalLayout />
            </DataTemplate>
            <DataTemplate x:Key="GridTemplate">
                <v:GridLayout />
            </DataTemplate>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <ContentControl Content="{Binding}">
            <ContentControl.Style>
                <Style TargetType="ContentControl">
                    <Setter Property="ContentTemplate" Value="{StaticResource SingleTextTemplate}" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Settings.Layout}" Value="Vertical">
                            <Setter Property="ContentTemplate" Value="{StaticResource VerticalTemplate}" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Settings.Layout}" Value="Horizontal">
                            <Setter Property="ContentTemplate" Value="{StaticResource HorizontalTemplate}" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Settings.Layout}" Value="Grid">
                            <Setter Property="ContentTemplate" Value="{StaticResource GridTemplate}" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ContentControl.Style>
        </ContentControl>
    </Grid>
</Window>
